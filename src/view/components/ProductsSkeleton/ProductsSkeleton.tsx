import React from "react";

const ProductsSkeleton = () => {
  return {
    //   product skeleton make with tailwinds
    /* <Row>
        {Array(12)
          .fill("")
          ?.map((product, index) => (
            <Col md={3} className="mb-3" key={index}>
              <Skeleton style={{ borderRadius: 10 }} height={320} />
            </Col>
          ))}
      </Row> */
  };
};

export default ProductsSkeleton;
